import React from "react";
import ReactLoading from "react-loading";

const LoadingIndicator = () => (
  <div className="" >
    <ReactLoading type="spin" color="#40739E" className="loader"/>
  </div>
);

export default LoadingIndicator;
